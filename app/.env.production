# production environment variables for the React frontend
# replace the URL below with the real location of your Django API server
# 
# For LOCAL TESTING: use http://127.0.0.1:8000/ (Make sure Django is running with: python manage.py runserver)
# For PRODUCTION: use the full https:// URL where your Django backend is hosted (e.g., Azure App Service)
#
REACT_APP_API_URL=http://mywebpage0946.live-website.com

# optional override for the photo upload URL, if it's different
#REACT_APP_PHOTO_URL=http://127.0.0.1:8000/Photos/
